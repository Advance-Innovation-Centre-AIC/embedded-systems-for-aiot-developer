# s05_pot_capsense.py - ลูกบิด + แถบสัมผัส + ฟิลเตอร์ EMA (ฉบับฝึกเติมโค้ด)
# วิธีรัน: 1) บนจอบอร์ด แตะการ์ด Playground ค้างไว้ตลอดคาบ
#          2) เติมช่องว่างทีละจุด รันทีละครั้ง แล้วดูว่าจอ "ตื่น" ขึ้นทีละส่วน
#          3) กด Program to Device แล้วหมุนลูกบิด กับลากนิ้วบนแถบทองแดง CapSense
#
# ไฟล์นี้ตั้งค่าเริ่มต้นไว้ให้แล้ว (pct = 0.0, slider = 0) จึงรันได้ตั้งแต่ยังไม่เติมอะไร
# แต่ตัวเลขจะนิ่งสนิท ใช้ตรงนี้เป็นเครื่องมือวัดความคืบหน้าของตัวเอง
#
# หน้าจอเขียนไว้ให้ครบแล้ว ไม่ต้องแตะ - งานของเราคือทำให้ค่าไหลเข้าไปในนั้น
# ดูที่จอ: การ์ดบนซ้ายคือค่าลูกบิดบนไม้บรรทัดของมันเอง การ์ดบนขวาคือเกณฑ์ที่ตั้งเองได้
#         พร้อมไฟสองดวง การ์ดล่างซ้ายคือแถบสัมผัส การ์ดล่างขวาคือค่าดิบเทียบค่ากรอง
#
# คาบนี้มีตัวอย่างเจ็ดไฟล์ที่ examples/s05/ แบ่งเป็นสามกอง ฝั่งลูกบิดคือ 03 04 05
# ฝั่งแถบสัมผัสคือ 01 02 และฝั่งตัวกรองคือ 06 07 ติดตรงไหนให้เปิดกองนั้นกองเดียวพอ

import ui
ui.screen()          # ล้าง widget เดิมทั้งหมด เริ่มจากจอว่างที่รู้แน่
import time
import sensors
import dsp

ui.clear()
time.sleep_ms(200)

# สีจัดสงวนไว้ให้สถานะผิดปกติเท่านั้น ค่าปกติเป็นตัวหนังสือขาวบนการ์ดสีเข้ม
COL_TEXT, COL_DIM = 0xFFFFFF, 0xA0B4CC
COL_CARD = 0x142240
COL_OK, COL_WARN, COL_BAD = 0x00E676, 0xFFC83D, 0xFF5252

# เกณฑ์เตือนเป็นค่าที่ผู้ใช้ตั้งเอง ไม่ใช่ค่าคงที่ที่ฝังอยู่ในโค้ด
TH_MIN, TH_MAX, TH_STEP = 10, 95, 5
th = 70              # เกณฑ์ตั้งต้น หน่วยเปอร์เซ็นต์

# --- ท่าที่ 1: บอร์ดนี้ไม่ต้องปลุกเซนเซอร์ ---
# บน Eva Kit บัสของลูกบิดกับแถบสัมผัสเป็นของคอร์จอ (CM55) คนเดียว
# ฝั่ง Python ขอค่าที่คอร์จออ่านเก็บไว้ให้ทุก 200 ms จึงเรียกอ่านได้เลยโดยไม่ต้อง init
# เรียก sensors.init() หรือ sensors.scan() บนบอร์ดนี้จะได้ OSError ทันที ไม่ใช่ค้าง
#
# หลังรีเซ็ต คอร์จอเริ่มตอบเรื่องเซนเซอร์ราว 13 วินาที การอ่านครั้งแรก
# จึงนิ่งได้ถึง 16 วินาที ครั้งถัดไปเร็วปกติ อุ่นเครื่องหนึ่งครั้งตรงนี้
# ให้การรอไปเกิดก่อนสร้างจอ ไม่ใช่ตอนที่นักเรียนกำลังจ้องตัวเลขอยู่
try:
    sensors.pot.read()
except OSError:
    print("คอร์จอยังไม่ตอบรอบแรก จะลองใหม่ในลูป")

ui.Label("แผงคุมลูกบิดกับแถบสัมผัส", x=16, y=6, color=COL_TEXT, value=24)

# --- ท่าที่ 2: ค่าที่วัดได้ ต้องมาพร้อมพิสัยของมันเอง ---
ui.Panel(x=12, y=38, w=468, h=170, color=COL_CARD, min=COL_DIM, max=12, value=1)
ui.Label("ลูกบิดเทียบพิสัย 0-100", x=26, y=44, color=COL_DIM, value=16)
# ui.Scale คือไม้บรรทัด ไม่มีเข็มและไม่รับ .value() ตัวที่ขยับคือ ui.Bar ที่วางทับ
pot_bar = ui.Bar(x=26, y=72, w=428, h=16, color=COL_OK, min=0, max=100, value=0)
pot_scale = ui.Scale(x=26, y=90, w=428, h=44, color=COL_TEXT, min=0, max=100)
pot_scale.ticks(11, 2)          # 11 ขีด ใส่ตัวเลขทุกขีดที่สอง = 0 20 40 60 80 100

ui.Label("เปอร์เซ็นต์ที่วัดได้", x=26, y=140, color=COL_DIM, value=14)
lbl_pct = ui.Label("0.0 %", x=26, y=162, color=COL_TEXT, value=28)
lbl_raw = ui.Label("ค่าดิบ 0", x=240, y=146, color=COL_DIM, value=16)
lbl_volt = ui.Label("โวลต์ 0.000 V", x=240, y=174, color=COL_DIM, value=16)

# ท่าเดียวกันครึ่งหลัง: เกณฑ์ที่ผู้ใช้ตั้งเอง กับไฟสองดวงที่บอกสถานะ
ui.Panel(x=492, y=38, w=288, h=170, color=COL_CARD, min=COL_DIM, max=12, value=1)
ui.Label("เกณฑ์เตือน (เปอร์เซ็นต์)", x=506, y=44, color=COL_DIM, value=16)
# spinbox เปล่า ๆ บนจอสัมผัส นิ้วเปลี่ยนค่าไม่ได้ ตัวที่เพิ่มลดจริงคือปุ่มสองปุ่มข้าง ๆ
sp_th = ui.Spinbox(x=506, y=70, w=120, h=46, color=COL_TEXT,
                   min=TH_MIN, max=TH_MAX, value=th)
sp_th.digits(2, 0)              # ไม่บอกจะเห็น 0070 เพราะค่าตั้งต้นคือสี่หลัก
btn_up = ui.Button("เพิ่ม", x=634, y=70, w=68, h=46, color=0x37474F, value=16)
btn_dn = ui.Button("ลด", x=710, y=70, w=60, h=46, color=0x37474F, value=16)

ui.Label("สถานะเทียบเกณฑ์", x=506, y=124, color=COL_DIM, value=14)
led_ok = ui.Led(x=510, y=148, w=32, h=32, color=COL_OK, value=1)
ui.Label("ต่ำกว่าเกณฑ์", x=548, y=154, color=COL_DIM, value=16)
led_bad = ui.Led(x=660, y=148, w=32, h=32, color=COL_BAD, value=0)
ui.Label("เกิน", x=698, y=154, color=COL_DIM, value=16)

# --- ท่าที่ 3: แถบเลื่อนสัมผัสและปุ่มทองแดงสองปุ่ม ---
ui.Panel(x=12, y=216, w=468, h=104, color=COL_CARD, min=COL_DIM, max=12, value=1)
ui.Label("แถบสัมผัสและปุ่มทองแดง", x=26, y=222, color=COL_DIM, value=14)
touch_bar = ui.Bar(x=26, y=244, w=318, h=14, color=COL_OK, min=0, max=100, value=0)
touch_scale = ui.Scale(x=26, y=258, w=318, h=44, color=COL_TEXT, min=0, max=100)
touch_scale.ticks(11, 2)
# ไฟแทนตัวหนังสือสี: ถ่ายจอเป็นขาวดำแล้วยังแยกออกว่าดวงไหนติด
led_b0 = ui.Led(x=380, y=244, w=28, h=28, color=COL_OK, value=0)
ui.Label("ปุ่ม 0", x=412, y=248, color=COL_DIM, value=14)
led_b1 = ui.Led(x=380, y=280, w=28, h=28, color=COL_OK, value=0)
ui.Label("ปุ่ม 1", x=412, y=284, color=COL_DIM, value=14)

# --- ท่าที่ 4: บรรทัดคุณภาพของค่า และการเทียบค่าดิบกับค่ากรองแล้ว ---
ui.Panel(x=12, y=328, w=468, h=60, color=COL_CARD, min=COL_DIM, max=12, value=1)
ui.Label("คุณภาพของค่า", x=26, y=334, color=COL_DIM, value=14)
lbl_health = ui.Label("อ่านค่าปกติ", x=26, y=356, color=COL_DIM, value=20)
health = "อ่านค่าปกติ"      # ข้อความล่าสุดของบรรทัดคุณภาพ คิดใหม่วินาทีละครั้ง

ui.Panel(x=492, y=216, w=288, h=172, color=COL_CARD, min=COL_DIM, max=12, value=1)
ui.Label("ค่าดิบเทียบค่าที่กรองแล้ว", x=506, y=224, color=COL_DIM, value=14)
raw_line = ui.Label("ดิบ    0.00 %", x=506, y=252, color=COL_TEXT, value=20)
ema_line = ui.Label("กรอง  0.00 %", x=506, y=284, color=COL_TEXT, value=20)
ui.Label("อัลฟา 0.2 คาบ 200 ms", x=506, y=316, color=COL_DIM, value=14)
ui.poll()

# ฟิลเตอร์ต้องเกิดนอกลูป เพราะมันต้องจำอดีต - สร้างในลูปเมื่อไร ค่าที่ได้จะเท่าค่าดิบเป๊ะ
# เติม: ema = dsp.EMA(alpha=0.2)
# 0.2 นี้แปลว่ากรองแรงแค่ไหน: examples/s05/06_ema_time_constant.py แปลง alpha เป็นวินาที
#   ด้วย tau = dt * (1-alpha)/alpha แล้ววาดเทียบกับ alpha ของขั้นก่อนหน้าให้เห็นบนกราฟเดียวกัน
#   ที่นั่นยังเตือนไว้ด้วยว่า ถ้าไม่ใส่ alpha ค่าตั้งต้นคือ 0.1 ไม่ใช่ 0.5 อย่างที่หลายคนเดา
pass

# ค่าเริ่มต้นสำหรับรอบก่อนเติมโค้ด ทำให้จอขึ้นครบแม้ตัวเลขยังไม่ขยับ
raw, pct, volts = 0, 0.0, 0.0
ema_pct = 0.0
slider = 0
last_sec = -1        # วินาทีที่เพิ่งเขียนตัวเลขลงจอ กันไม่ให้เขียนถี่กว่าวินาทีละครั้ง

# --- ท่าที่ 5: ลูปหลักและจังหวะที่ปลอดภัย ---
while True:
    fresh = True
    try:
        # ทุกบรรทัดที่อ่านค่า = ถามคอร์จอหนึ่งรอบ อยากได้ทั้งชุดในการถามครั้งเดียว
        # ให้ใช้ sensors.snapshot() ซึ่งคืน dict ที่มีคีย์ bmi270 capsense pot
        raw = sensors.pot.read()
        # โวลต์บนบอร์ดนี้คำนวณจากเปอร์เซ็นต์ ไม่ได้วัดมาตรง ๆ และแรงดันอ้างอิง 3.3 V
        # ที่ใช้คูณยังไม่มีใครเอามิเตอร์ไปยืนยัน ใช้ดูแนวโน้มได้ แต่อย่าอ้างเป็นค่าวัด
        volts = sensors.pot.voltage()

        # เติม: pct = sensors.pot.percent()
        # สงสัยว่าเลขดิบกับเปอร์เซ็นต์กับโวลต์ผูกกันอย่างไร: examples/s05/05_adc_counts_to_volts.py
        #   ลากเส้นทั้งสองเส้นคู่กัน ค่าจริงที่เรียบ กับค่าที่ ADC เห็นซึ่งเป็นบันได
        #   และชี้กับดักตัวหารสองตัว 2**n กับ 2**n - 1 ที่สลับกันแล้วเพี้ยนน้อยจนจับไม่ได้
        pass

        # เติม: slider = sensors.capsense.slider()
        # ปล่อยนิ้วแล้วค่าไม่กลับเป็นศูนย์ นั่นไม่ใช่บั๊ก: examples/s05/01_capsense_dimmer.py
        #   วาดค่าดิบคู่กับระดับที่สั่งไฟจริง ให้เห็นว่าตอนไม่มีนิ้วแตะ ค่าดิบลงไปติดลบ
        #   และเตือนว่า slider() == 0 ไม่ได้แปลว่าปล่อยนิ้ว มันแปลว่าแตะที่ปลายซ้ายก็ได้
        pass

        b0, b1 = sensors.capsense.buttons()
    except OSError:
        # อ่านพลาดหนึ่งรอบไม่ควรทำให้จอดับทั้งหน้า ใช้ค่าเดิมไปก่อน
        # แต่ต้องบอกให้คนดูรู้ว่าตัวเลขบนจอตอนนี้ไม่ใช่ค่าปัจจุบันแล้ว
        fresh = False
        b0, b1 = 0, 0

    # เติม: pot_bar.value(int(max(0, min(100, pct))))
    # แถบสั่นยิบ ๆ ทั้งที่ไม่มีใครแตะลูกบิด: examples/s05/03_pot_setpoint_deadband.py
    #   เพิ่มกติกาข้อเดียวว่า "ขยับไม่ถึงเท่านี้ ถือว่าไม่ได้ขยับ" แล้วค่าที่รายงานกลายเป็น
    #   ขั้นบันไดที่นิ่ง ใช้ตอนต้องส่งค่าออกเครือข่าย จะลดจำนวนข้อความลงหลายเท่า
    pass

    # ครอบขอบเขตก่อนส่งเข้า widget เสมอ ค่าที่มาจากภายนอกโปรแกรมเราไม่มีใครรับประกันให้
    touch_bar.value(max(0, min(100, slider)))

    # เติม: ema_pct = ema.update(pct)
    # ป้อนเข้า เอาค่าที่กรองแล้วออก - แค่นั้นจริง ๆ แต่ EMA ไม่ได้ชนะทุกงาน
    #   examples/s05/07_median_beats_mean.py ยิงค่าหลุดเข้าไปหนึ่งค่า แล้วให้ดูว่าค่าเฉลี่ย
    #   ถูกลากตามไปหลายรอบ ส่วน median แบนสนิท ถ้างานของทีมเจอค่าหลุดบ่อย ให้เลือกจากภาพนั้น
    pass

    # แถบกับไฟขยับได้ทุกรอบ เพราะตาคนอ่าน "ตำแหน่ง" ได้โดยไม่ต้องหยุดอ่าน
    # หนึ่งดวงติดเท่านั้น แผงที่ติดพร้อมกันหลายดวงคือแผงที่อ่านไม่ออก
    over = ema_pct >= th
    led_ok.value(0 if over else 1)
    led_bad.value(1 if over else 0)
    led_b0.value(1 if b0 else 0)
    led_b1.value(1 if b1 else 0)

    # เติม: events = ui.poll()
    # กฎเหล็กข้อ 1: ต้องเรียกทุกรอบ ไม่งั้น widget บางตัวจะถูกซ่อนไว้นานถึงสองวินาที
    #   และปุ่มเพิ่ม/ลดเกณฑ์จะกดไม่ติดเลย เพราะ event ไม่มีทางเดินทางกลับมาถึงเรา
    events = []
    pass

    # --- ปุ่มเพิ่ม/ลดเกณฑ์: คนละปุ่มกันคนละทิศ ไม่ใช่ปุ่มเดียวสลับ ---
    for ev in events:
        if ev.get("type") != "clicked":
            continue
        if ev.get("handle") == btn_up.id():
            th = min(TH_MAX, th + TH_STEP)
            sp_th.value(th)
        elif ev.get("handle") == btn_dn.id():
            th = max(TH_MIN, th - TH_STEP)
            sp_th.value(th)

    # ตัวเลขที่คนต้องอ่าน เขียนใหม่ไม่เกินวินาทีละครั้ง และอยู่ตำแหน่งเดิมเสมอ
    sec = time.ticks_ms() // 1000
    if sec != last_sec:
        last_sec = sec
        lbl_pct.text("{:.1f} %".format(pct))
        lbl_raw.text("ค่าดิบ {}".format(raw))
        lbl_volt.text("โวลต์ {:.3f} V".format(volts))
        raw_line.text("ดิบ    {:.2f} %".format(pct))
        ema_line.text("กรอง  {:.2f} %".format(ema_pct))
        # ตั้งสีก่อนแล้วค่อยเขียนข้อความ สองบรรทัดนี้เป็นคำสั่งคนละครั้งข้ามคอร์
        if not fresh:
            lbl_health.color(COL_WARN)
            health = "ค่าค้าง ตัวเลขข้างบนคือค่าล่าสุด"
        elif over:
            lbl_health.color(COL_BAD)
            health = "เกินเกณฑ์ {} เปอร์เซ็นต์".format(th)
        else:
            lbl_health.color(COL_DIM)
            health = "อ่านค่าปกติ ต่ำกว่าเกณฑ์ {}".format(th)

    # --- ชีพจรของช่องทางข้ามคอร์: ส่งบรรทัดสถานะซ้ำทุกรอบโดยตั้งใจ ---
    # ฝั่งจอมีตัวจับเวลาสองจังหวะ โหมดเร็ว 5 ms กับโหมดปกติ 200 ms และมันจะอยู่
    # โหมดเร็วต่อไปอีก 500 ms ทุกครั้งที่ได้รับคำสั่ง "เขียนข้อความ" หรือ "ย้าย/ย่อ/เปลี่ยนสี"
    # แต่ .value() ของแถบกับไฟ "ไม่" ปลุกโหมดนั้น ลูปที่อัปเดตเฉพาะแถบกับไฟจึงเงียบเกิน
    # ครึ่งวินาที แล้วจอจะถอยไปโหมดช้า ภาพกระตุกทั้งที่โค้ดไม่ได้เปลี่ยนสักบรรทัด
    # ข้อความที่ส่งไปนี้เท่าเดิมเกือบทุกรอบ และเฟิร์มแวร์ไม่วาดซ้ำถ้าข้อความไม่เปลี่ยน
    # เราจึงจ่ายแค่ค่าส่งข้ามคอร์ ไม่ได้จ่ายค่าวาดใหม่
    lbl_health.text(health)

    time.sleep_ms(200)   # จังหวะที่ผ่านการทดสอบสำหรับจอที่มีของหลายชิ้น
